// Q19. Dinner Guests:  
// Working with one of the programs from Exercises 14 through 18, print a message indicating 
// the number of people you are inviting to dinner.

// Working on Q14)

let guest_name :string[] = ['Ali', 'Ahmad', 'Abdul'];
for (let i = 0; i < guest_name.length; i++) {
    console.log(`Hi ${guest_name[i]}, You are invited on Dinner.`);
};
console.log(`Total ${guest_name.length} guests are coming.`);
